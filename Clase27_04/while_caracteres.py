frase = input("Escribe la frase: ")
letra = input("Escrib una letra: ")
#variable = "" #Variable vacía

index=0
contador=0
while (index < len(frase)):
    if (frase[index] == letra):
        contador += 1
    #print(frase[index])
    index += 1
    


#for letras in frase:
 #   print(letras)